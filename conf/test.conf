include "base.conf"

# Circumvent error during play app tests: "A metric named jvm.attribute.vendor already exists"
metrics.jvm=false

actorTimeout = 1500ms

akka.loglevel = error

auth {
  api-keys = [
    { "name": "test-key", "key": "test-api-key" }
  ]
}
